import React from 'react';
import PageTemplate from './PageTemplate';
import { Typography } from '@mui/material';

const Ventas = () => {
  return (
    <PageTemplate title="Gestión de Ventas">
      <Typography>Contenido de Ventas</Typography>
    </PageTemplate>
  );
};

export default Ventas; 