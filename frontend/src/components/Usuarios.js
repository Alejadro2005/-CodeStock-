import React from 'react';
import PageTemplate from './PageTemplate';
import { Typography } from '@mui/material';

const Usuarios = () => {
  return (
    <PageTemplate title="Gestión de Usuarios">
      <Typography>Contenido de Usuarios</Typography>
    </PageTemplate>
  );
};

export default Usuarios; 